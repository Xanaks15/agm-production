version: '3.8'

services:
  # ==================================
  # API GATEWAY (NGINX)
  # ==================================
  nginx:
    image: nginx:alpine
    container_name: api-gateway
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infra/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - ms-auth
      - ms-periodos-materias
      - ms-docentes-alumnos
      - ms-calificaciones
      - ms-asistencias
      - ms-notificaciones
      - ms-reportes

  # ==================================
  # MS-1: Auth
  # ==================================
  auth_db:
    image: postgres:15-alpine
    container_name: auth_db
    restart: always
    environment:
      POSTGRES_USER: agm_auth_user
      POSTGRES_PASSWORD: ${AUTH_DB_PASSWORD}
      POSTGRES_DB: agm_auth_db
    volumes:
      - auth_db_data:/var/lib/postgresql/data

  ms-auth:
    build:
      context: ./ms-auth
      dockerfile: Dockerfile
    container_name: ms-auth
    restart: always
    environment:
      - DATABASE_URL=postgresql://agm_auth_user:${AUTH_DB_PASSWORD}@auth_db:5432/agm_auth_db
      - API_HOST=0.0.0.0
      - API_PORT=8001
      - GRPC_HOST=0.0.0.0
      - GRPC_PORT=50051
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - NOTIFICACIONES_GRPC_HOST=ms-notificaciones
      - NOTIFICACIONES_GRPC_PORT=50056
    depends_on:
      - auth_db

  # ==================================
  # MS-2: Periodos y Materias
  # ==================================
  periodos_db:
    image: postgres:16-alpine
    container_name: periodos_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${PERIODOS_DB_PASSWORD}
      POSTGRES_DB: agm_periodos_db
    volumes:
      - periodos_db_data:/var/lib/postgresql/data

  ms-periodos-materias:
    build:
      context: ./ms-periodos-materias
      dockerfile: Dockerfile
    container_name: ms-periodos-materias
    restart: always
    environment:
      - PROJECT_NAME=MS-2 Periodos y Materias
      - SERVICE_NAME=ms-periodos-materias
      - ENVIRONMENT=production
      - REST_HOST=0.0.0.0
      - REST_PORT=8002
      - GRPC_HOST=0.0.0.0
      - GRPC_PORT=50052
      - DATABASE_URL=postgresql+asyncpg://postgres:${PERIODOS_DB_PASSWORD}@periodos_db:5432/agm_periodos_db
      - CORS_ALLOWED_ORIGINS=https://app.agm.com,https://api.agm.com
      - MS_AUTH_GRPC_HOST=ms-auth
      - MS_AUTH_GRPC_PORT=50051
      - MS_DOCENTES_ALUMNOS_GRPC_HOST=ms-docentes-alumnos
      - MS_DOCENTES_ALUMNOS_GRPC_PORT=50053
    depends_on:
      - periodos_db

  # ==================================
  # MS-3: Docentes y Alumnos
  # ==================================
  db-docentes:
    image: postgres:15-alpine
    container_name: db-docentes-alumnos
    restart: always
    environment:
      POSTGRES_USER: agm_docentes_user
      POSTGRES_PASSWORD: ${DOCENTES_DB_PASSWORD}
      POSTGRES_DB: agm_docentes_alumnos_db
    volumes:
      - postgres_data_ms3:/var/lib/postgresql/data

  ms-docentes-alumnos:
    build:
      context: ./ms-docentes-alumnos
      dockerfile: Dockerfile
    container_name: ms-docentes-alumnos
    restart: always
    environment:
      - DATABASE_URL=postgresql://agm_docentes_user:${DOCENTES_DB_PASSWORD}@db-docentes:5432/agm_docentes_alumnos_db
      - REST_PORT=8003
      - MS1_HOST=ms-auth
      - MS1_PORT=50051
      - MS2_HOST=ms-periodos-materias
      - MS2_PORT=50052
      - MS6_HOST=ms-notificaciones
      - MS6_PORT=50056
    depends_on:
      - db-docentes
      - ms-auth

  # ==================================
  # MS-4: Calificaciones
  # ==================================
  calificaciones_db:
    image: postgres:16-alpine
    container_name: calificaciones_db
    restart: always
    environment:
      POSTGRES_USER: calificaciones_user
      POSTGRES_PASSWORD: ${CALIFICACIONES_DB_PASSWORD}
      POSTGRES_DB: agm_calificaciones_db
    volumes:
      - calificaciones_data:/var/lib/postgresql/data

  ms-calificaciones:
    build:
      context: ./ms-calificaciones
      dockerfile: Dockerfile
    container_name: ms-calificaciones
    restart: always
    environment:
      - SERVICE_NAME=ms-calificaciones
      - ENV=production
      - REST_HOST=0.0.0.0
      - REST_PORT=8004
      - GRPC_HOST=0.0.0.0
      - GRPC_PORT=50054
      - DATABASE_URL=postgresql://calificaciones_user:${CALIFICACIONES_DB_PASSWORD}@calificaciones_db:5432/agm_calificaciones_db
      - MS_ALUMNOS_GRPC_URL=ms-docentes-alumnos:50053
    depends_on:
      - calificaciones_db
      - ms-docentes-alumnos

  # ==================================
  # MS-5: Asistencias
  # ==================================
  asistencias_db:
    image: postgres:15-alpine
    container_name: asistencias_db
    restart: always
    environment:
      POSTGRES_USER: agm_user
      POSTGRES_PASSWORD: ${ASISTENCIAS_DB_PASSWORD}
      POSTGRES_DB: agm_asistencias_db
    volumes:
      - asistencias_db_data:/var/lib/postgresql/data

  ms-asistencias:
    build:
      context: ./ms-asistencias
      dockerfile: Dockerfile
    container_name: ms-asistencias
    restart: always
    environment:
      - ENVIRONMENT=production
      - DATABASE_URL=postgresql+asyncpg://agm_user:${ASISTENCIAS_DB_PASSWORD}@asistencias_db:5432/agm_asistencias_db
      - QR_SECRET_KEY=${QR_SECRET_KEY}
      - CORS_ORIGINS=${CORS_ORIGINS}
      - QR_TTL_SECONDS=20
      - SESSION_DURATION_MINUTES=10
      - PRESENT_LIMIT_MINUTES=5
      - AUTH_GRPC_HOST=ms-auth
      - AUTH_GRPC_PORT=50051
      - MATERIAS_GRPC_HOST=ms-periodos-materias
      - MATERIAS_GRPC_PORT=50052
      - ALUMNOS_GRPC_HOST=ms-docentes-alumnos
      - ALUMNOS_GRPC_PORT=50053
    depends_on:
      - asistencias_db
      - ms-auth
      - ms-periodos-materias
      - ms-docentes-alumnos

  ms-asistencias-grpc:
    build:
      context: ./ms-asistencias
      dockerfile: Dockerfile
    container_name: ms-asistencias-grpc
    restart: always
    command: python -m app.grpc_server.asistencias_server
    environment:
      - ENVIRONMENT=production
      - DATABASE_URL=postgresql+asyncpg://agm_user:${ASISTENCIAS_DB_PASSWORD}@asistencias_db:5432/agm_asistencias_db
      - QR_SECRET_KEY=${QR_SECRET_KEY}
      - CORS_ORIGINS=${CORS_ORIGINS}
      - QR_TTL_SECONDS=20
      - SESSION_DURATION_MINUTES=10
      - PRESENT_LIMIT_MINUTES=5
      - AUTH_GRPC_HOST=ms-auth
      - AUTH_GRPC_PORT=50051
      - MATERIAS_GRPC_HOST=ms-periodos-materias
      - MATERIAS_GRPC_PORT=50052
      - ALUMNOS_GRPC_HOST=ms-docentes-alumnos
      - ALUMNOS_GRPC_PORT=50053
    depends_on:
      - asistencias_db
      - ms-auth
      - ms-periodos-materias
      - ms-docentes-alumnos

  # ==================================
  # MS-6: Notificaciones
  # ==================================
  notificaciones_db:
    image: postgres:15-alpine
    container_name: notificaciones_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${NOTIFICACIONES_DB_PASSWORD}
      POSTGRES_DB: agm_notificaciones_db
    volumes:
      - notificaciones_data:/var/lib/postgresql/data

  ms-notificaciones:
    build:
      context: ./ms-notificaciones
      dockerfile: Dockerfile
    container_name: ms-notificaciones
    restart: always
    environment:
      - APP_ENV=production
      - DEBUG=false
      - REST_HOST=0.0.0.0
      - REST_PORT=8006
      - GRPC_HOST=0.0.0.0
      - GRPC_PORT=50056
      - DATABASE_URL=postgresql+psycopg://postgres:${NOTIFICACIONES_DB_PASSWORD}@notificaciones_db:5432/agm_notificaciones_db
      - SMTP_HOST=smtp.gmail.com
      - SMTP_PORT=587
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASSWORD=${SMTP_PASSWORD}
    depends_on:
      - notificaciones_db
    volumes:
      - ./proto:/app/proto

  # ==================================
  # MS-7: Reportes
  # ==================================
  reportes_db:
    image: postgres:15-alpine
    container_name: reportes_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${REPORTES_DB_PASSWORD}
      POSTGRES_DB: agm_reportes_db
    volumes:
      - reportes_data:/var/lib/postgresql/data

  ms-reportes:
    build:
      context: ./ms-reportes
      dockerfile: Dockerfile
    container_name: ms-reportes
    restart: always
    environment:
      - DATABASE_URL=postgresql://postgres:${REPORTES_DB_PASSWORD}@reportes_db:5432/agm_reportes_db
    depends_on:
      - reportes_db

volumes:
  auth_db_data:
  periodos_db_data:
  postgres_data_ms3:
  calificaciones_data:
  asistencias_db_data:
  notificaciones_data:
  reportes_data:
